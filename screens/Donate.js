import React, { useState } from "react";

import { StyleSheet, Text, View, TouchableOpacity, StatusBar, FlatList, Button } from 'react-native';

import { SafeAreaView } from "react-native-safe-area-context";
const Donate = ({navigation}) => {
    return (
        // <SafeAreaView>
        //     <Text>Donate</Text>
        //     <Button title="Exit" onPress={() => navigation.navigate('Charitable', {screen: 'Map'})} />
        //     <Button title="Mission" onPress={() => navigation.push('Mission')} />
        // </SafeAreaView>
        <View>
            <Text>Donate</Text>
        </View>
    )
}

export default Donate;